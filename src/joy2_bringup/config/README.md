# joy2_bringup Config Directory

This directory is intentionally present so that the `joy2_bringup` package can install configuration files when needed (e.g., ros2_control or launch parameter YAMLs).  
Currently no specific configuration files are required.